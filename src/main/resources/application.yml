server:
  port: 8080
spring:
  h2:
    console:
      enabled: true
  datasource:  
    url: jdbc:h2:mem:ShoppinCart
    username: sa
    password: 'password'  
    driverClassName: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true